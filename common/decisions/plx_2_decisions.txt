PLX_Integration_decisions = {
	PLX_Integrate_State = {
		icon = eng_trade_unions_demand
		allowed = { always = no }
		
		#available = {
		#	FROM = {
		#		has_political_power < 30 
		#	}
		#}

		visible = {
			FROM = {
				is_owned_by = ROOT
				NOT = { is_core_of = ROOT }
			}
		}

		cost = 20
		days_remove = 120
		cancel_if_not_visible = yes

		state_target = yes
		##targets = { 10 }
		#target_trigger = {
		#	FROM = {
		#		is_owned_by = ROOT
		#		NOT = { is_core_of = ROOT }
		#	}
		#}

		ai_will_do = {
			factor = 1000
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove PLX_Integrate_State target: [From.GetName]"
			hidden_effect = { remove_state_claim = FROM }
			add_state_core = FROM
		}
	}	
}
